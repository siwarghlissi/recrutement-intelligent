# Application
spring.application.name=auth-service

# PostgreSQL Configuration (ESSENTIEL)
spring.datasource.url=jdbc:postgresql://localhost:5433/auth_db




spring.datasource.username=root
spring.datasource.password=11168477
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Kafka Configuration (v�rifiez le host)
spring.kafka.bootstrap-servers=kafka:9092  # Utilisez 'kafka' au lieu de 'localhost' dans Docker

# JWT Configuration
jwt.secret=c3ByaW5nYm9vdGp3dHNlY3VyZWtleTEyMzQ1Njc4OTA=
jwt.expiration=86400

jwt.refresh-expiration=604800  

# Security (configuration temporaire pour le dev)
spring.security.user.name=admin
spring.security.user.password=admin
management.endpoints.web.exposure.include=*

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE